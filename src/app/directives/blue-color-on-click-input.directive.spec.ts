import { BlueColorOnClickInputDirective } from './blue-color-on-click-input.directive';

describe('BlueColorOnClickInputDirective', () => {
  it('should create an instance', () => {
    const directive = new BlueColorOnClickInputDirective();
    expect(directive).toBeTruthy();
  });
});
